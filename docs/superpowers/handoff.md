# Agent Handoff

> 本文件只保留当前状态和最近的重要交接。
> 历史记录归档在 `docs/superpowers/handoff-archive.md`，通常无需阅读；仅在追溯旧决策、兼容性背景或历史问题时按需查阅。
> 归档中的“下一步”可能已经失效，不应视为当前任务；当前状态以代码、本文件和最新设计/计划为准。

## 当前状态

- **日期**: 2026-08-19
- **分支**: `main`
- **当前提交**: `0.8.1` 发布提交，已推送（`0.9.0` 版本元数据本次未提交未推送）
- **版本**: `0.9.0`，已同步 `version.json`、`CHANGELOG.md`、README 与本交接记录
- **最近完成**: 版本元数据更新至 `0.9.0`，收录 115 大目录读取分页重构与官方搜索（加载更多、断连自动降页重试）、刮削整理重命名批量合并与深层路径修复、刮削任务“等待执行”修复与启动恢复、预览性能优化、NFO 媒体信息分类、自动识别保留手动搜索/绑定、搜索框交互优化
- **已有验证**: 完整 580 项 unittest、项目 `compileall`、改动 JS `node --check` 与 `git diff --check` 均通过；本地无 Docker 重建与真实 115 大目录/大批量整理实测
- **下一步**: Docker daemon 启动后按项目代理配置 `docker compose up -d --build`，用真实 115 账号实测大目录读取与“加载更多”、官方搜索、1242 部合集内深层文件夹整理（含重命名位置与批量请求数）、刮削任务重启自动恢复与任务中心表现，并复核 NFO 保留与搜索框交互


## 最近重要交接

- 2026-08-19 | `main` 未提交 | 发布元数据更新至 `0.9.0`：同步 version.json、CHANGELOG、README 与交接状态，收录 115 大目录分页+官方搜索、重命名批量合并、深层整理路径修复、任务“等待”修复、预览性能、NFO 媒体信息分类、自动识别保留手动搜索/绑定、搜索框交互优化。完整 unittest 580 项通过；未提交未推送。
- 2026-08-19 | `main` 未提交 | 修复 NFO 文件被误判为广告：`.nfo` 从 `SCRAPER_BATCH_AD_EXTENSIONS` 移除，新增 `SCRAPER_BATCH_INFO_EXTENSIONS` 与 `info` 分类——NFO 作为媒体信息文件始终保留原名、不参与整理、即使开启"删除广告文件"也不删除，预览提醒文案改为"媒体信息 NFO/广告/说明/标准封面等"；剧集集数解析的排除集合同步加入 `info`。更新 1 项既有断言并新增 1 项回归（开启删除广告时 NFO 仍保留、无删除动作）；完整 unittest 580 项、compileall、`git diff --check` 均通过。Docker 未重建，刷新页面即可生效。
- 2026-08-19 | `main` 未提交 | 修复批量整理自动识别完成时清空用户正在输入的手动搜索/手动绑定：`identifyBatch` 在自动搜索结束后无条件重置 `batchSearchState`/`batchBindings`/`batchIncluded`，而 `scanBatch` 发起扫描时已重置过，二次重置会抹掉用户在自动识别期间打开并输入的手动搜索框（以及已手动绑定的条目）。删除 `identifyBatch` 内的重复重置，只保留自动匹配结果写入。新增 1 项前端回归（自动识别无匹配时保留手动搜索词/绑定/勾选）；完整 unittest 578 项、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，刷新页面后即可生效。
- 2026-08-19 | `main` 未提交 | 大目录读取 IncompleteRead 进一步加固：目录列表在断连/超时类错误重试仍失败时，自动缩小单页大小（默认 500→250→100）重试，更小响应不易被服务端掐断（仅对可重试错误缩小，405 等确定性错误直接抛出）；`resolve_115_entry_by_name` 改为复用带重试的分页读取（顺带修复二次规范化把 id 当文件 id 的隐患，测试覆盖），STRM 按名查找两个函数补上断连重试封装。新增 1 项缩小页大小回归；完整 unittest 577 项、compileall、`git diff --check` 均通过。Docker 未重建，需重启服务后在大目录实测。
- 2026-08-19 | `main` 未提交 | 修复刮削任务卡"等待执行"：根因是后台任务只有单线程事件循环，同步的 `run_scraper_job` 会占住它，前面有任务（如慢的 115 请求）时新任务只能排队；且无启动恢复，重启也不会续跑。改法：刮削任务/回滚改走独立单线程执行器 `submit_scraper_job`/`submit_scraper_rollback`（不再占用共享后台循环，也不被监控/目录树任务堵住），意外异常由 `_run_scraper_job_guarded` 标为失败避免卡"执行中"；新增 `requeue_scraper_jobs_on_startup` 在启动时把 pending 任务重新入队、把 running 任务标为"服务重启，任务中断，请重新执行"，并接入 startup。新增 1 项启动恢复回归；完整 unittest 576 项、compileall、`git diff --check` 均通过。Docker 未重建，需重启服务后验证等待任务自动续跑。
- 2026-08-19 | `main` 未提交 | 修复大目录下刮削预览"没有可执行项"的定位与健壮性：计划期/执行期的名称查找（`_target_name_exists`/`_walk_existing_folder`/`_ensure_folder_from_base`）原来会触发完整模式全量扫描（分页重构后会把大目录所有页拉完），改为有界分页查找（每页 300、找到即停、最多 80 页，与 `resolve_115_entry_by_name` 一致），并新增 `_get_scraper_entries_page`/`_scraper_page_has_more`；前端"预览没有可执行项"toast 改为直接显示前 2 条冲突/失败原因（`data.issues`），不再只给"请处理冲突后再试"。真实案例"1242 部电影合集"内深层文件夹：怀疑合集里已有同名/同标题条目被新分页查找暴露（此前 300 条截断看不到），需重试后按 toast 具体文案确认。新增 0 项（既有 575 项全过），更新 9 处测试桩 `fake_list` 接受 offset/limit；compileall、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，需重启服务/刷新页面后重试验证。
- 2026-08-19 | `main` 未提交 | 刮削整理重命名批量合并（官方接口一次多条目）：新增 `rename_115_entries`（`webapi.115.com/files/batch_rename` 一次传多个 `files_new_name[id]=名字`，单条 `rename_115_entry` 改为其包装）；执行器重构为 `_execute_scraper_job_batch_forward`——原地改名（文件夹/同目录文件）按父目录分组合并（每 100 条一次请求）、纯移动按目标目录合并 `files/move`、移动+改名保持"临时改名→移动→改最终名"三步安全模式但每步按阶段批量提交；目标目录解析按路径去重、冲突预检按目标目录共享缓存去重；失败时尽力恢复临时名；每个动作仍独立记录状态/监控事件/回滚字段。新增 5 项回归（多条目一次提交、单条包装、原地改名合并、移动按目标分组、三阶段），更新 8 处既有测试 mock 为批量入口；完整 unittest 575 项、compileall、`git diff --check` 均通过。Docker 未重建，真实 115 大批量整理请求数/风控实测待补。
- 2026-08-19 | `main` 未提交 | 刮削页搜索框 UX 修复：点击“应用/清除”或按回车执行搜索后不再自动关闭搜索弹层，改为保持展开并聚焦输入框，可直接连续修改关键词，按 Esc 或再点搜索图标才会关闭。新增 `openSearchPopover`，更新 1 项前端回归测试断言；完整 unittest 570 项、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，浏览器交互实测待补。
- 2026-08-19 | `main` 未提交 | 修复刮削重命名后文件被放到错误层级（致命）：根因是文件动作的目标目录锚点错位——前端在祖先目录（如根）选中库文件夹被拆分为二级条目时，`base_cid`（当前浏览目录=根）与后端兜底改写后的 `base_path`（所选文件夹的父目录=一级）不一致，执行层用 `base_cid` + 相对父路径 `新片名` 建目录，把内容建到了根目录、与一级同级。修复：文件夹模式下文件目标锚定在“所选文件夹的父目录”（新增 `_scraper_folder_organize_root`/`_scraper_file_folder_anchor`），且文件动作的 `target_parent_path` 改为完整挂载路径（`dirname(target_path)`），计划期 `_walk_existing_folder` 与执行期 `_ensure_folder_from_base` 统一从挂载根（`0`）用完整路径解析，两个锚点不再打架。新增 4 项回归（二级文件夹重命名留在父目录内、一级文件夹留在根下、深层拆分场景文件目标正确、执行层从根建完整父路径），更新 1 项既有 `target_parent_path` 断言为完整路径；完整 unittest 570 项、compileall、`git diff --check` 均通过。Docker 未重建，真实 115 拆分/深层整理实测待补。
- 2026-08-18 | `main` 未提交 | 115 大目录读取分页重构 + 官方搜索：`list_115_entries_payload` 从“单页 300、整包读取”改为按服务端 offset 逐页拉取（默认每页 500、完整模式最多 80 页），每页对 `IncompleteRead`/断连/超时做指数退避重试（HTTP 状态错误如 405 不重试）；返回真实 `count/next_offset/has_more/entries_complete`，完整模式合并后仍按“文件夹在前、名称排序”供内部按名/按 id 查找，`limit>0` 走分页模式，`folders_only` 分页会连续扫页集齐文件夹避免漏页。新增官方搜索 `search_115_entries`（`webapi.115.com/files/search`），刮削页搜索框改为服务端搜索（失败自动回退分页列表+本地过滤）。resource/scraper 接口透传 offset/limit 并返回分页元数据；刮削页新增“加载更多条目”，资源导入/订阅/监控文件夹选择器新增“加载更多文件夹”。新增 11 项后端分页/重试/搜索测试、5 项前端分页测试，并更新 1 项搜索行为测试；完整 unittest 566 项、compileall、改动 JS `node --check` 与 `git diff --check` 均通过。Docker 未重建，真实 115 大目录读取/服务端搜索实测待补。
- 2026-08-18 | `main` 已推送 | 发布 `0.8.1`：同步 version.json、CHANGELOG、README 与交接状态，收录磁力/电驴离线任务官方完成检测驱动刷新与进度展示、前端显示修复（推荐页回顶、刮削页手机端操作栏单行、日志按钮日间配色、目录树任务卡片移动端布局与图标化按钮）、订阅任务扫描链接弹窗一键粘贴按钮、文件浏览器统一组件补齐修改时间（资源导入分享目录选择器新增时间列 + 后端 compact 透传 `modified_at` 等字段，115/夸克可显示，目标目录与订阅分享目录时间列不再为 `--`）。新增 4 项 compact 回归，完整 unittest 550 项、compileall、改动 JS `node --check`、git diff --check 均通过；时间列与粘贴按钮经本地真实浏览器渲染实测（日夜主题）；Docker 未重建。下一步：Docker 重建后实测容器内离线任务/时间列/粘贴按钮表现。
- 2026-08-18 | `main` 未提交 | 目录树任务 4 个操作按钮图标化：生成并同步=单箭头环形刷新（绿）、全量重写=双箭头环形刷新（红，形状+颜色双重区分强制）、编辑=铅笔（中性灰）、删除=垃圾桶（红）；38×38 方形图标按钮（SVG 18px），复用订阅任务卡片的交互（`title` 悬浮提示 + `aria-label`，运行中禁用保持），按钮基类补齐后夜间/日间配色沿用既有 tone 类。新增 2 项前端回归（图标按钮结构与提示、图标尺寸 CSS），完整 unittest 546 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；360/1280px 与日夜主题本地真实浏览器渲染实测（4 按钮 38×38、提示正确、run 绿/full 红/edit 灰/delete 红）。Docker 未重建。
- 2026-08-18 | `main` 未提交 | 修复目录树任务卡片手机端布局：任务卡片外层原是 `flex flex-wrap items-start justify-between`，信息区 `flex-1` 但按钮区 `shrink-0`，手机端按钮不换行导致信息区被压缩成窄条、卡片被顶高；改为订阅任务同款 `flex flex-col lg:flex-row lg:items-start lg:justify-between`，手机端信息全宽、按钮独立一行，≥1024px 恢复左右并排。新增 1 项前端回归（卡片布局类），完整 unittest 544 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；360/1280px 本地真实浏览器渲染实测（360px 信息区 326px 全宽、按钮独立行、卡片 157px 高；1280px 恢复并排）。Docker 未重建。
- 2026-08-18 | `main` 未提交 | 修复监控/订阅/任务三个日志头部按钮在日间模式变成“纯文本”：根因是日间主题全局规则 `html.theme-day .bg-slate-800` 会把按钮底色拍平成卡片表面色，而这三个按钮原本只依赖 `bg-slate-800/text-slate-300` 工具类且没有边框。现统一改用 `.log-header-btn` 类（夜间深灰底、日间浅灰底 + 描边，各配 hover），模板同时去掉会被拍扁的工具类。新增 2 项前端回归（模板类名、日夜两套配色），完整 unittest 543 项、compileall 与 git diff --check 均通过；日夜主题本地真实浏览器渲染实测按钮均有可见底色；Docker 未重建。
- 2026-08-18 | `main` 未提交 | 刮削页手机端操作栏 7 个按钮一行显示：区间选择/扫描监控/批量整理 在 ≤760px 切换为短文案（区间/扫描/整理），选择区网格 6 列改 7 列，按钮内边距 0.12rem、字号 0.66rem，360/375px 本地静态渲染实测单行不折行；桌面端保持完整文案（按钮内双 span，CSS 按断点切换）。新增 3 项前端回归（7 列网格与几何参数、双标签切换、批量整理双标签结构），完整 unittest 541 项、compileall 与 git diff --check 均通过；Docker 未重建，真实页面 320px 老机型复核待补（如需覆盖可把“重命名”手机端显示为“改名”）。
- 2026-08-18 | `main` 未提交 | 修复资源推荐页翻页后停留在页面中间：翻页点击后 `renderLoadingGrid` 先把网格替换为 12 张骨架卡，页面高度骤减，浏览器把滚动位置钳制到页面中部，新一页内容渲染后仍停留中间；`goToRecommendationPage` 现于翻页分发前显式 `window.scrollTo({top:0, behavior:'smooth'})` 回顶。新增 2 项前端回归（回顶源码顺序 + vm 实测翻页触发回顶），完整 unittest 540 项、compileall、改动 JS `node --check` 与 `git diff --check` 均通过；Docker 未重建，浏览器实测待补。
- 2026-08-02 | `d8c455c` | 资源卡片、频道摘要和频道管理统一使用 18 类展示标签；光鸭保持独立展示类型，但操作仍按普通直链处理。
- 2026-08-02 | `ed43c53` | 手机竖屏资源卡片操作区改为底部四按钮单排；设置页明确磁力和电驴链接仅支持 115 网盘。
- 2026-08-02 | 未提交 | 发布元数据更新到 `0.5.2`；静态与自动化验证完成，等待 Docker 重建后的真实页面复核。
- 2026-08-02 | 未提交 | 移除资源中心频道分组标题上的链接类型汇总标签，保留单个资源卡片类型标记；容器重建后实际页面确认频道标题类型标签为 0、卡片仍显示类型标签，123 项 unittest、JS 语法和编译检查通过。
- 2026-08-02 | 未提交 | 订阅任务网盘标签接入全局配色，并在标题栏展示最近成功命中的相对日期、详情展示精确时间；定向 20 项与完整 128 项 unittest、JS 语法、Python 编译、差异检查及 Docker 日夜/手机页面复核均已通过。
- 2026-08-02 | `main` | 发布元数据升至 `0.5.3`，收录订阅任务标签与最近命中日期功能；验证版本同步、测试、编译和差异检查后提交。
- 2026-08-03 | `main` | 刮削标准命名重复识别优化：完整路径无变化条目不再加入改名计划或查询目标目录，执行层保留同名跨目录移动行为；新增 5 项回归测试，完整 133 项 unittest、项目 compileall、刮削 JS 语法和 git diff 检查通过。下一步：如需真实网盘/页面验证，按项目要求重建 Docker 后复核。
- 2026-08-04 | `main` 未提交 | 电驴外链仅识别 `telegra.ph` 页面并在后端限制同域跳转；直接 ED2K 保持导入，普通 HTTP(S) 与光鸭仅展示且禁用下载，既有五个网盘分享转存不变。新增前后端回归测试，完整 138 项 unittest、Python 编译、3 个资源 JS 语法检查和差异检查通过。下一步：启动 Docker daemon 后执行 `docker compose up -d --build`，复核资源卡与 Telegraph 导入页面。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.5`，同步版本说明、变更日志、README 与当前交接状态。下一步：启动 Docker daemon 后执行 `docker compose up -d --build`，复核资源卡与 Telegraph 导入页面。
- 2026-08-04 | `main` 未提交 | 导入任务列表改为 20 条累计窗口与统一请求序号控制，修复加载更多、轮询缩页和过期响应覆盖；新增已完成/失败单条记录删除并在最后关联任务删除后恢复资源状态。新增 14 项前后端回归测试，完整 152 项 unittest、Python 编译、8 个改动 JS 语法检查和差异检查通过；Docker daemon 未启动，容器重建及任务中心日夜模式实测待补。
- 2026-08-04 | `main` 未提交 | 修复频道同步漏掉最新资源：`fetch_telegram_channel_post_samples` 现按发布时间/消息号倒序收集，避免第一页资源超过目标数时把最旧的一批入库；资源任务弹窗头部仅保留“任务列表”标题。新增 2 项取样回归测试，完整 154 项 unittest、Python 编译和差异检查通过；真实 TG 频道实测返回最新帖。下一步：启动 Docker daemon 后重建容器，在资源页对活跃频道复核“最近资源”时间。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.6`：收录频道同步最新资源修复与任务弹窗标题去重，同步 version.json、CHANGELOG、README 与交接状态。完整 154 项 unittest、compileall 与差异检查通过。下一步：启动 Docker daemon 后重建容器，复核资源页活跃频道“最近资源”时间与任务弹窗标题。
- 2026-08-04 | `main` 未提交 | 修复任务中心“删除记录”按钮在浅色主题下不可见：该按钮的 `bg-red-600` 工具类未包含在旧 Tailwind 生成文件中，重新生成 `tailwind.generated.css` 后已补上；仅静态样式文件改动，git diff 检查通过。下一步：启动 Docker daemon 后重建容器，在任务中心日夜模式复核删除按钮。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.7`：收录任务中心删除按钮可见性修复，同步 version.json、CHANGELOG、README 与交接状态。下一步：启动 Docker daemon 后重建容器，在任务中心日夜模式复核删除按钮。
- 2026-08-07 | `main` 未提交 | 修复频道同帖多条直连 ED2K 仅保存第一条：保存弹窗按频道原帖链接顺序收集、校验并去重全部 ED2K 文件，默认全选，Telegraph 解析与批量提交接口不变。新增频道多链接和前端收集回归，完整 158 项 unittest、Python compileall、相关 JS 语法检查与 git diff 检查通过。下一步：重建 Docker 后在真实频道资源中确认多文件勾选与保存。
- 2026-08-07 | `main` 未提交 | 频道同帖资源改为保存结构化 `extra.resource_links`，兼容旧 `all_links`；卡片按全部链接显示标签，复制可选全部已识别链接，下载/转存只接受已启用且可导入的候选，保持单链接、ED2K 批量和原有导入接口语义。补充混合网盘、磁力、ED2K、展示型直链、旧缓存、二级链接筛选及锚点原帖顺序回归；完整 164 项 unittest、Python compileall、3 个资源 JS 语法和 git diff 检查通过。Docker 已从当前源码重建，真实多链接卡片的复制选择、夸克单候选导入及日/夜主题流程均已复核，控制台无相关错误。下一步：待用户决定是否提交。
- 2026-08-07 | `main` 未提交 | 发布元数据更新至 `0.5.9`，同步 `version.json`、`CHANGELOG.md`、README 与当前交接状态，收录频道同帖多链接兼容修复。下一步：待用户决定是否提交。

## 记录规则

- 仅在完成重要功能、重要修复，或需要其他 agent 接手时追加记录。
- 每条写明日期、分支或提交、完成内容、验证结果、卡点和唯一明确的下一步，避免记录调试过程。
- 涉及 CLI 可操作能力（刮削整理选项、监控任务配置、接口变更等）的改动，需同步补齐 `cli.py` 命令支持与 README CLI 文档，并在记录中注明。
- 已完成、被后续决策取代或不再影响当前工作的记录移入归档。

- 2026-08-09 | `main` 未提交 | 修复资源中心频道同步完成后的首页刷新：compact 轮询发现完成状态时，在搜索框为空的情况下触发一次完整资源状态刷新；有搜索词时保留当前搜索结果。新增 3 项前端回归测试，完整 167 项 unittest、Python compileall、资源 JS 语法检查和 git diff 检查通过。Docker daemon 未启动，容器重建与真实页面复核待补。下一步：启动 Docker daemon 后按 `docker compose up -d --build` 复核空搜索同步刷新和带搜索词保留结果。
- 2026-08-09 | `main` | 发布版本更新至 `0.5.10`，同步 version.json、CHANGELOG、README 与交接记录，收录资源中心同步完成刷新修复和 3 项回归测试。完整 167 项 unittest、compileall、资源 JS 语法检查和差异检查已通过；Docker daemon 未启动。下一步：启动 Docker daemon 后重建容器复核两条同步刷新路径。
- 2026-08-09 | `main` 未提交 | 精准 STRM 同步根因修复：批量刮削的 `old_path/new_path` 统一为 115 挂载点下完整路径，旧待执行任务依据 `base_path` 确定性兼容；处理器先校验删除/新增/替换计划，路径不完整或父路径与父目录 ID 矛盾时保留旧 STRM。文件级操作不清理空目录，已知文件夹复用索引，未知文件夹只同步已知部分并标记 `manual_required`，手动监控成功覆盖后清除提示。新增真实三文件批量改名回归和矛盾父目录保护；定向 94 项通过。下一步：重新运行完整 unittest、compileall、改动 JS 语法和 diff 检查；Docker 可用时重建后再做明确的小目录真实 115 验证。
- 2026-08-10 | `main` 发布 | 精准 STRM 同步安全收口：连续 move/rename 合并不再依赖 `source_action`，跨目录链同步回溯父 CID，只有从未处理的 forward/rollback 回到原路径时才收敛为空操作；处理中断恢复的事件标记本地状态不确定并按顺序重放。所有文件夹索引逐项做旧根/新根/任务范围校验，跨任务清单按文件大小过滤范围判断完整性；共享 STRM 不允许不同远端 URL 静默覆盖，事件级 journal 在本地写入或数据库提交失败时恢复旧文件；启动恢复利用经过校验的 create/copy 目标 CID，未确认 delete 遇同 ID 改名时替换旧索引，脏文件夹改名后保留“需手动监控”。`0.5.12` 元数据已同步；定向 108 项、完整 253 项、compileall、两个改动 JS 的语法检查和 diff 检查均通过；Docker daemon 未运行，未执行容器重建或真实 115 验证。下一步：启动 Docker 后在明确的小目录执行三文件改名实测。
- 2026-08-10 | `main` 未提交 | 精准 STRM 同步日志补充实际本地删除/生成明细，批量文件按事件顺序相邻输出，文件夹以单行摘要显示；版本升至 `0.5.13`。5 项定向测试、compileall、JSON 解析和差异检查通过；未运行完整测试，Docker daemon 未运行。下一步：用户确认后再决定是否提交或推送。
- 2026-08-10 | `main` 未提交 | 修复批量刮削父目录与子文件同批改名导致“移动父目录路径与目录 ID 不一致”：执行与回退都按已成功目录变换计算当前路径，事件按父目录 ID 记录为 rename/move，文件夹清单投影同批已确认子项；增加处理器修订号，使 #184 这类旧处理器达到上限的失败事件在重建启动后自动重排队一次，并按同批已完成父目录事件恢复旧路径。89 项精准同步定向测试、完整 258 项 unittest、compileall 和 diff 检查通过；Docker daemon 未运行。下一步：启动 Docker 后按项目代理配置重建，在该电影目录确认 #184 自动完成且只保留最终 STRM。
- 2026-08-11 | `main` 未提交 | 发布元数据更新至 `0.5.14`，同步 `version.json`、`CHANGELOG.md`、README 与当前交接状态，收录批量刮削父目录/子文件同批改名修复和旧事件单次升级重试。89 项精准同步定向测试、完整 258 项 unittest、compileall、版本 JSON 与差异检查通过；Docker daemon 未运行。下一步：启动 Docker 后按项目代理配置重建，在该电影目录确认 #184 自动完成且只保留最终 STRM。
- 2026-08-11 | `main` 未提交 | 精准同步边界改为路径-only：网盘执行层继续使用 ID，刮削确认成功后传给文件夹监控的事件快照只保留文件名、旧路径和新路径；本地 rename/move 不再校验父目录 ID。刮削本地 STRM 同步失败会恢复 journal、写日志后删除事件，不累计重试；普通监控事件仍按原策略重试，启动时清理历史同类失败事件。新增/调整路径冲突、无 ID 移动、路径-only 快照、一次性失败日志与历史清理回归。精准同步 92 项、目录监控 24 项、完整 unittest 261 项、compileall、git diff --check 均通过；Docker daemon 未运行，未执行容器重建或真实 115 验证。下一步：Docker 可用时按项目代理配置重建，在明确的小目录验证刮削重命名后的 STRM 路径和失败日志。
- 2026-08-11 | `main` 发布 | 修复目录树 MD5 跳过顺序：所有源先完成下载和哈希，只有存在变化时才初始化数据库；全部未变化时不回放路径缓存、不解析目录树、不检查或写入 STRM，部分变化时仍回放未变化源缓存并只解析变化源。版本更新至 `0.5.16`，同步 version.json、CHANGELOG、README 与交接记录；目录树定向 6 项、完整 unittest 263 项、compileall 与 git diff --check 通过。未执行 Docker 重建或真实 115 目录树验证。下一步：Docker 可用时用同一目录树连续执行两次，确认第二次下载哈希后直接结束。
- 2026-08-12 | `main` 发布 | 修复刮削绑定后标准剧集文件“无法识别集数”：自动集数解析改为文件或父路径带明确季号时优先使用该季号，完全无季号时才用“未识别时默认季号”兜底，与手动集数覆盖路径一致；不再因页面默认季号为 1 而把 `S03E07` 这类文件按 season_mismatch 丢弃。版本更新至 `0.5.17`，同步 version.json、CHANGELOG、README 与交接记录；新增 5 项回归测试（含 en-dash 文件名、父目录季号、无季号兜底、多季连续编号映射），完整 unittest 268 项、compileall 与 git diff --check 均通过。下一步：Docker 可用时重建后，在刮削页绑定多季剧集并用 `S03E07` 标准文件名实测生成预览。
- 2026-08-12 | `main` 未提交 | 刮削页新增“批量整理”：先勾选条目再点“批量整理”，只对勾选的文件夹/文件识别（文件夹=一个条目并递归收集媒体文件，散文件=单条目；同批去重并剔除已含在所选文件夹内的条目），不再整目录自动扫描；批量自动识别（关键词清洗 + TMDB 多候选打分，≥80 自动匹配、≥55 建议、其余待人工搜索绑定），确认后整批生成合并预览并复用任务中心执行/回滚；批量预览内手动补集数会联动重算整批计划；合并计划带跨条目目标路径重复检测，监控目录内的整理变更继续走路径-only 精准 STRM 同步（端到端测试验证无远端列目录）。同时把“影视”等通用词加入刮削关键词噪声表，避免父目录名压过真实片名；批量整理界面补全日间模式配色（置信度徽标、类型标签等），日/夜两套主题经无头浏览器逐层背景合成对比度审计，徽标与文字对比度均 ≥6.4。新增 8 项批量整理回归测试，完整 unittest 276 项、compileall、改动 JS `node --check`、模板结构校验与 git diff --check 均通过；未执行 Docker 重建或真实 115 验证。下一步：Docker 可用时重建，在真实小目录勾选文件夹/散文件执行批量整理，确认只识别勾选项、待确认改绑、执行后监控目录 STRM 更新。
- 2026-08-12 | `main` 未提交 | 修复批量识别选错片名的两个根因：`_strip_extension` 把发布组括号尾 `[YTS.GG - YTS.BZ]` 的 `.BZ]` 误当扩展名截断，改为只裁剪纯字母数字且 1-5 位的真实扩展名；批量主查询不再用多来源加权（父目录“电影小库”曾靠 26+25+10 分压过文件夹名），改为“条目自身名称清洗标题优先，父目录名仅作噪声过滤，文件内标题兜底”，并把“电影小库/影视库/电影大全”等容器词加入通用噪声表。新增 3 项回归（YTS 尾括号不截断、英文文件夹不再被父库名抢占、通用文件夹名回退到文件标题），完整 unittest 279 项、compileall 与 git diff --check 通过。下一步：与批量整理一起在 Docker 重建后真实验证。
- 2026-08-12 | `main` 未提交 | 修复批量整理面板“改绑/接受/搜索/绑定”按钮点击无响应：`handleClick` 的 `if (!actionButton) return;` 守卫在批量按钮分发之前，导致没有 `data-scraper-action` 的批量按钮被提前吞掉；已把批量按钮处理移到守卫之前，并新增源码顺序回归测试防止回退。完整 unittest 280 项、改动 JS `node --check`、compileall 与 git diff --check 通过。
- 2026-08-12 | `main` 未提交 | 批量识别改为按发布名结构解析而非词表逐条修补：`_extract_scraper_title_candidates` 用媒体标签/年份/季集等“技术标记”做标题边界（标记前是标题，`S01E01` 开头时取标记后），先剥离站点前缀与方括号组前缀，`www.UIndex.org - Secrets to Kill For 2025 NORDiC 1080p WEB-DL DDP5.1 H.264-ADDICTION`、`Movie.Name.2024.1080p.BluRay.x264-GROUP` 等统一提取为纯标题；主查询无结果时按降级查询重试，但降级命中一律扣 25 分/级，只进“建议”不会自动选片。同时保留尾部发布组剥离与 `5 1` 声道兼容。新增 6 项结构解析/降级安全回归，完整 unittest 284 项、compileall 与 git diff --check 通过。
- 2026-08-12 | `main` 未提交 | 识别核心升级为“guessit 结构化解析 + 候选打分重做”：`_extract_scraper_title_candidates` 改为 guessit 主解析、手写解析兜底合并（requirements.txt 新增 `guessit>=3.8,<4`）。guessit 自动取路径最后一段（修复“电影小库/Governor (2026)…”父目录被并进标题）、识别 `1x03` 季集结构并参与剧集判定、剥离站点前缀；多部曲拼回（`Dune Part 2/Two`）、中英混排拆分候选（`John Wick Chapter 4`/`疾速追杀`）。打分重做：年份为硬约束（冲突重罚，且顶部候选年份冲突时禁止自动选片）、标题/别名/译名精确命中大权重（得分接近时补拉 TMDB 详情破平局）、popularity 仅做同分 tie-break，降级查询仍只建议。新增 5 项回归（路径条目取末段、`1x03` 剧集判定、多部曲/混排候选、Robocop 2014 不配 1987、别名破平局），完整 unittest 289 项、compileall 与 git diff --check 通过；Docker daemon 未运行，容器重建与真实 115 识别验证待补。
- 2026-08-12 | `main` 未提交 | 批量整理按文件类型分类处理非影视文件：视频正常改名；字幕保留语言/编码标记并自动加序号保证唯一（`Denmark.dan.srt` → `片名 (2026).dan.srt`，多条同语言自动追加 ` (2)`），TV 字幕带集数与语言后缀；海报跟随片名，广告图（`YTS.GG - Official site`、www 域名、logo 等）与标准封面（`poster.jpg`/`folder.jpg`）保留原名；广告/说明文件（txt/url/html/nfo 等）不改名不删除，仅统计并在预览提醒。新增 7 项回归（文件分类、字幕语言后缀、多字幕唯一命名、TV 字幕带集数、广告/海报保留、批量忽略统计），完整 unittest 296 项、compileall 与 git diff --check 通过；Docker 未重建、真实 115 验证待补。
- 2026-08-12 | `main` 未提交 | 手动识别与批量识别合并为单一“识别整理”入口：工具栏只保留一个按钮，点击后统一走“自动识别优先”（勾选 1..N 条都进批量面板，单条即一行）；每行保留“改绑/搜索绑定”手动入口，点击展开行内识别卡，复用大爆炸选词（`ScraperPathSelection` token 选词 → 填入关键词 → TMDB 搜索 → 绑定），选中/清空/完成选词/重新选词均可，搜索函数统一为 `fetchTmdbSearch`（手动与批量共用）。旧的 `/scraper/identify`、`/scraper/rename-plan` 与手动面板代码保留兼容、不再从菜单暴露。新增 3 项回归（工具栏单入口、handleClick 合并分发、行内选词与共享搜索），完整 unittest 299 项、改动 JS `node --check`、compileall 与 git diff --check 均通过；Docker 未重建，浏览器交互实测待补。
- 2026-08-12 | `main` 未提交 | 修复剧集集数解析两个根因：(1) `RESOURCE_EPISODE_CODE_REGEX` 的 `(?<!\d)` 会把 `Jade.2026` 结尾的字母 `e` 误当 `E 2026`（`Movie.2012` 同理），改为 `(?<![A-Za-z0-9])` 词边界，`Pursuit.of.Jade.2026.EP02...` 正确解析为 S01E02，EP02/EP03 不再撞成同目标；(2) 纯数字序号文件（`01.mkv`/`02.mkv`）此前能解析集数但 `_looks_like_tv` 判成电影不走剧集流程，新增“文件夹内 ≥2 个纯数字序号文件按剧集处理”的判定，年份（1900-2099）不参与计数避免年份电影目录误判。新增 3 项回归（`e` 结尾单词不误判集数、EP02 无年份抢占、纯数字剧集判定），完整 unittest 302 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 剧集目录实测待补。
- 2026-08-12 | `main` 未提交 | 修复合并入口后丢失命名开关与“整理后旧文件夹残留”两类问题：(1) 把“命名选项”（保留清晰度/片源/编码、标题语言、默认季号、季集识别方式、写入 TMDB ID、Season 子文件夹、同步重命名文件夹）从旧手动面板搬进批量面板，`renderBatch` 新增 `syncBatchOptionControls` 按条目的文件夹/剧集属性控制开关显隐与可用性；(2) 文件夹条目不勾选“同步重命名文件夹”时，文件改为整理在源文件夹内部（`organize_inside_source_folder`），不再创建兄弟级新文件夹、不再留下空的旧文件夹；勾选时行为不变（文件夹改名 + Season 01 + 文件移入）。端到端（文件夹改名 + Season 01 + STRM 同步）验证旧 STRM/索引全部清理。新增 4 项回归（批量面板含命名选项、renderBatch 同步开关、文件夹重命名开/关两种计划路径），完整 unittest 306 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 实测待补。
- 2026-08-13 | `main` 未提交 | 支持“数字 + 单集标题”剧集命名（`1. The Hedge Knight.mkv`、`01 - Title.mkv`、`02_Title.mkv`）：`_extract_numeric_episode_from_filename` 新增“1-3 位数字 + 分隔符（`.`/`-`/`_`/括号/全角冒号/集话）+ 标题文字”模式，纯数字与“数字+质量后缀”两条旧路径也统一排除 1900-2099 年份（`2012.1080p.mkv`、`2012.mkv` 不再误判为集数），同时保持 `12 Monkeys`（空格无分隔符）不误判。文件夹名里的 `S01` 季号识别此前已支持，`A.Knight.of.the.Seven.Kingdoms.S01.../1. The Hedge Knight.mkv` 现可正确得到 S01E01。新增 2 项回归（数字+标题集数提取、整条自动集数解析），完整 unittest 308 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 剧集目录实测待补。
- 2026-08-13 | `main` 未提交 | 移除工具栏“退出识别”按钮，预览态只保留“退出预览”：合并入口后“退出识别”（清识别状态+清预览）与“退出预览”（清预览）在预览态同时出现且行为等价，属于重复按钮；旧手动面板头部的关闭图标与 `clear-identify` 动作保留兼容。新增 2 项回归（模板仅剩面板头部关闭图标、renderSelection 不再管理工具栏退出识别按钮），完整 unittest 309 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 内置常见中文发布站点词条并在标题清洗中过滤（不依赖具体网址）：新增 `SCRAPER_CN_AD_SITE_PHRASES`（高清剧集网/电影天堂/免费影视网等）+ 结构性规则“XX网/站/论坛/吧/组 + 发布/首发/分享/出品/压制/制作/整理”，同一短语后面跟任何 www 域名都能命中；在前缀剥离、任意位置清洗、纯广告名噪声过滤、尾部 token 剥离四个环节生效，`高清剧集网发布 剧名.S01E01.mkv` 现只识别 `剧名`，`蜘蛛侠纵横宇宙` 等正常片名不受影响。新增 2 项回归（广告短语前后缀/括号形态清洗、纯广告名噪声过滤），完整 unittest 311 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复批量行内选词与多文件夹开关两个问题：(1) 批量行内“大爆炸选词”恢复滑动/拖拽区间选择——新增 `beginBatchPathSelection`/`continueBatchPathSelection`/`syncBatchPathTokenDom`，pointerdown/move/up 手势与手动面板一致，拖动过程只更新 token 高亮、松开后刷新查询词，点击单个词块同样可用；(2) 多文件夹选择时“同步重命名文件夹/写入 TMDB ID/Season 子文件夹”开关不再被旧手动面板同步逻辑禁用——`syncIncludeTmdbIdControl`/`syncSeasonSubfolderControl`/`syncFolderRenameControl`/`syncSeasonControl`/`syncEpisodeModeControl` 在批量面板打开时跳过，改由 `syncBatchOptionControls` 按批量条目统一控制。新增 4 项回归（手势函数与 pointer 事件接线、旧同步函数批量面板保护、多文件夹 tmdbid 重命名目标、handleClick 不再点击切换词块），完整 unittest 314 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，浏览器滑动选词实测待补。
- 2026-08-13 | `main` 未提交 | 预览行配色区分文件夹：可执行（准备就绪）的文件夹行新增 `is-ready-dir` 类，底色由绿色改为淡蓝色（夜间 `rgba(14,116,144,0.16)`、日间 `#eff6ff`），文件保持绿色，批量预览多选时一眼可分辨文件夹；文件夹冲突行仍走琥珀色“需处理”。新增 2 项回归（预览行文件夹类名、日夜两套配色存在），完整 unittest 316 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 预览态收敛工具栏：生成预览后隐藏区间选择/重命名/复制/移动/删除/识别整理等文件操作按钮（`renderSelection` 按 `hasPlan` 切换 `hidden`），预览界面只保留整理相关操作；“退出预览”按钮补充 `scraper-danger-soft` 红色危险底色。新增 1 项回归（预览隐藏文件操作按钮 + 退出预览红色样式），完整 unittest 317 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 刮削管理页 URL 记忆当前位置：只编码网盘类型与文件夹路径（`#tab=scraper&provider=115&path=...`，不含 cid 长数字），切换网盘/进入目录/返回上级时用 `history.replaceState` 同步（不产生历史记录、不触发路由循环）；刷新页面时 `restoreScraperLocation` 按路径逐段走回原目录，某段失效则停留在最近可访问位置。新增 1 项回归（URL 编解码函数、导航同步、初始化恢复接线、不含 cid 参数），完整 unittest 318 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，浏览器刷新实测待补。
- 2026-08-13 | `main` 未提交 | 预览态恢复“返回绑定界面”能力：新增“修改绑定”按钮（仅预览且存在批量识别上下文时显示），点击 `reopenBatchPanel` 直接重新打开批量识别面板且保留已识别条目与绑定（不重新扫描），用户可改绑/重新选词搜索后再次生成预览；无批量上下文时回退为重新发起识别。新增 1 项回归（按钮存在、handleClick 映射、保留上下文不重置），完整 unittest 319 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定界面高亮与等待动效：(1) 已绑定条目统一绿色高亮——自动匹配与手动绑定/采纳建议都显示绿色徽标与行底色（`is-auto` 徽标 + `is-included` 绿底），不再出现“手动绑定后徽标仍是灰色”的割裂感；(2) 点击识别后的等待状态加入动态加载：`scraper-busy-spinner` 旋转动画 + “正在识别中，请不要关闭页面”提示（日夜两套配色），避免用户误以为卡住。新增 1 项回归（徽标统一绿色、busy 分支含 spinner、日夜两套 spinner/绿底样式），完整 unittest 320 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复中英混排与字幕标签污染关键词：`男子心如钻[全30集][简繁英字幕].Strong.Will...` 这类名字现在 (1) 清洗掉 `简繁英字幕/中英字幕/特效字幕` 等字幕标签与括号内残留网址（嵌入域名正则要求“标签+分隔符+后缀”结构，不再误伤 `Treme`/`Name`/`NoTV` 这类单词）；(2) 中英混排候选拆成独立关键词（中文优先、英文次之、合并名最后兜底），搜索不再把“男子心如钻 Strong Will”当整条查；(3) 全角括号前缀 `【XX字幕组】` 在 guessit/手写解析前统一剥离。新增 2 项回归（混排拆分+字幕标签清洗、全角括号前缀剥离），完整 unittest 321 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 预览提醒内容可见化：生成预览的 toast 不再只报“含 N 个提醒”，会把前两条提醒原文拼进提示（超长截断）；预览列表顶部新增 `scraper-plan-warning-bar` 提醒条，逐条展示全部提醒内容（日夜两套配色），配合行内提醒更直观。新增 1 项回归（toast 含提醒原文、预览顶部提醒条、日夜两套样式），完整 unittest 322 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 扩展常见附属信息噪声词库：新增 `SCRAPER_COMMON_NOISE_PHRASES`（无字片源/无字幕/无水印/无删减/未删减/加长版/导演剪辑版/剧场版/重制版/修复版/国语版/粤语版/双音轨/多音轨/中英双语/特效中字/提取码/磁力链接/百度网盘/夸克网盘/阿里云盘等 30+ 复合短语），标题清洗任意位置去除、纯噪声名过滤，尾部 token 补充 片源/原盘/全集/完结/连载；词条采用复合短语避免误伤真实片名（`无字天书`/`独家记忆` 不受影响）。同时修复“匹配加载中无动态效果”：批量识别（扫描完成、条目已显示）阶段顶部也显示 `scraper-busy-spinner` 旋转动画与“正在匹配 TMDB 条目，请不要关闭页面”。新增 2 项回归（噪声短语清洗+真实片名保留、有条目时 busy 分支显示 spinner），完整 unittest 324 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新增“整理时删除广告文件”内置功能（默认关闭，需勾选才生效）：批量选项新增 `scraper-delete-ad-files` 开关；`_is_scraper_ad_file` 把广告扩展名（txt/url/html/nfo/torrent 等）与广告图片（含官网/www 域名文件名）识别为广告文件；开启后 `build_scraper_rename_plan` 为广告文件生成 `delete` 动作（预览行红色“删除”徽标、目标显示“（删除广告文件）”，执行确认弹窗提示删除数量），执行走 `_delete_provider_entries` + 监控删除事件，删除进入网盘回收站、任务回退跳过删除动作；`_load_scraper_job` 把 plan_json 中的动作字段（如 delete）合并回数据库行保证持久化。关闭时行为不变（广告文件仍保留原名不删）。新增 5 项回归（广告文件分类、计划生成删除动作开/关、任务执行删除分支、删除持久化、前端开关/预览/确认文案），完整 unittest 328 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 删除与回收站恢复待测。
- 2026-08-13 | `main` 未提交 | 补充验证广告删除覆盖 `.url/.html` 等扩展名（`Polskie-Torrenty.eu – Darmowe Torrenty — kopia.url`、`wejdź po więcej — kopia.html` 复现均生成删除动作），并新增批量路径下 `delete_ad_files` 选项传递到条目计划的回归测试。完整 unittest 329 项、compileall 与 git diff --check 均通过；Docker 未重建，需重建容器后实测删除。
- 2026-08-13 | `main` 未提交 | 批量识别加载动效去重：删除列表顶部的独立转圈进度条，加载中转圈改为内联在状态栏文字前（`scraper-busy-spinner inline`），成功/失败/加载信息统一只由顶部状态栏呈现。同时扩展附属噪声词：新增 `地址发布页/发布地址/永久地址/备用地址/最新地址/官网地址/防走丢/收藏本站/最新网址/发布页` 等短语，并把独立词 `电影` 加入清洗词表（`斯特林角 电影 地址发布页` 现只识别 `斯特林角`，`电影往事` 等真实片名不受影响）。新增 2 项回归（状态栏内联转圈且无独立进度条、发布页短语与电影词清洗），完整 unittest 330 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新增“库根拆分扫描”（`split_folders`）：批量整理选项新增“把所选文件夹按子目录拆分扫描”，选中整个影视库时可按路径在识别前拆成多个条目——直接含媒体的子文件夹=一个条目、散落媒体文件=单条目、通用分类容器（电影/电视剧/动漫/资源/合集/美剧等 `SCRAPER_LIBRARY_CONTAINER_KEYS` + 通用词）递归下探（最多 4 层）、其他只有子文件夹的文件夹按作品条目处理（含 Season 子目录）；**分层失败或没有媒体的文件夹跳过不处理**并给出“已跳过”提示。不勾选时保持“整个文件夹当一个条目”的旧行为。新增 3 项回归（库根拆分含分类下探/Season 结构/空夹跳过、不拆分保持单条目、前端开关与扫描参数），完整 unittest 333 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 库目录实测待补。
- 2026-08-13 | `main` 未提交 | 库根拆分改为自动判定并移除手动开关：`_looks_like_scraper_library_root` 自动判断选中文件夹是否像媒体库根（文件夹名是影视库/电影/剧集等容器词 → 直接拆分；否则看子项：含容器子目录、或 ≥2 个非 Season 子文件夹各自含媒体 → 拆分；直接含媒体或只有 Season 子目录 → 按单个作品条目处理），不再需要用户先识别失败再开开关重扫；后端 `split_folders` 参数保留为强制开关，前端不再传。新增/调整 4 项回归（影视库名自动拆分、自定义多作品库自动拆分、单作品/多季剧集保持单条目、前端无手动开关），完整 unittest 335 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 拆分方式改为识别弹窗顶部的两段式控件：默认“自动判断”，两个胶囊按钮“整个文件夹 / 按子目录拆分”可直接切换，切换后自动重新扫描（不再先识别失败再补救）；后端 `scan_scraper_batch_items` 新增 `split_mode`（auto/single/split），`split_folders=True` 兼容等价 split。新增 4 项回归（single 强制单条目、split 强制拆分、控件位于弹窗顶部且扫描参数传递、handleClick 分发在守卫之前），完整 unittest 338 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新旧名称完全一致且路径无变化的项不再进入任务队列：计划层新增 `unchanged_rows`（文件夹/文件“无变化”行，包含旧名/旧路径/条目归属），预览里以灰色“无变化 / 保持原名，已跳过”行展示、复选框禁用、不可执行；批量计划汇总时带上 `item_name` 区分条目；任务执行层增加防御性快速跳过（`old_path == new_path` 时不做任何远程调用/限速等待，直接标记 skipped）。新增 4 项回归（计划无变化行不入 actions、批量汇总带条目名、预览渲染无变化行、无变化快速跳过），完整 unittest 341 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 散落单集文件按剧集合并识别：`scan_scraper_batch_items` 对选中的散落媒体文件做 `_group_scraper_loose_tv_files` 分组——同目录内 ≥2 个文件提取出相同标题且带集数特征时合并成一个“剧集条目”（识别/绑定只做一次），电影/独立文件保持单条目；合并条目携带 `entries`（全部文件），`build_scraper_batch_plan` 把 `entries` 传给重命名计划，**预览/计划仍按文件逐个生成动作，显示与原来一致**（20 个文件 = 20 行）。新增 2 项回归（20 集散文件合并为 1 条目、合并条目计划展开为每文件动作），完整 unittest 343 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定成功的条目右侧显示封面图：`build_tmdb_task_binding` 补充 `tmdb_poster_url`（手动绑定路径也能拿到封面），批量识别面板已绑定行新增 `scraper-batch-match-wrap` 布局，右侧渲染 `scraper-batch-poster` 缩略封面（54×78，日夜两套配色），自动匹配与手动绑定均生效。新增 2 项回归（绑定保留封面字段、绑定行渲染右侧封面与样式），完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定行封面与文本布局优化：封面加大到 66×96 并在行内垂直居中；绑定信息改为上下两行结构（徽标+类型一行，标题单独一行 `scraper-batch-title` 超出省略不换行），消除徽标/标题/类型挤在一行导致的换行撑空行问题。新增断言覆盖 `scraper-batch-title` 结构与样式，完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 批量绑定行封面改为整行右列铺满：行网格改为「勾选 / 文本 / 封面」三列，封面 `scraper-batch-poster-col` 随整行高度拉伸（`height:100%` + `aspect-ratio:2/3`，上限 168px），不再只占中间匹配段；左侧文本间距收紧（`scraper-batch-main` gap 0.32rem、`match-info` gap 0.3rem），消除上下空行与文本过散。新增断言覆盖 `scraper-batch-poster-col` 结构，完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 批量绑定弹窗（及旧识别弹窗）标题与右上角关闭按钮改为吸顶：`.scraper-batch-dialog .scraper-panel-head`（同 `.scraper-identify-dialog`）`position: sticky; top: 0`，滚动内容时头部始终可见，背景用近不透明的面板色并加底部描边（日夜两套），用户随时可关闭无需回滚顶部。新增 1 项回归（吸顶样式日夜存在），完整 unittest 346 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修正批量弹窗吸顶露缝问题：弃用 sticky+负 margin 方案，改为「头部固定 + 内容区滚动」结构——`.scraper-batch-dialog` 改 flex column + `overflow:hidden`，头部 `flex:0 0 auto` 固定（带底部描边，日夜配色），新增 `.scraper-batch-dialog-body`（`overflow-y:auto` + `min-height:0`）包裹状态栏/拆分开关/列表/选项/操作，弹窗滚动只在内容区进行，顶部不再露缝。新增 1 项回归（模板 body 包裹、头部固定与内容区滚动样式），完整 unittest 346 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 监控任务新增“新增资源自动刮削整理”开关（`auto_scrape_on_new`，默认关，任务设置界面可勾选）：监控扫描时对比上一轮 `monitor_files` 索引，捕获新增媒体文件（含 115 fid/大小/路径），扫描完成后经 `_auto_scrape_new_media_items` 自动走“扫描→识别→计划→任务”管道，**只对置信度 ≥80 的自动匹配条目执行一次整理**，suggest/manual 跳过并在日志提示；任何失败仅记录日志不重试，不做半成品/防抖等额外处理（按用户要求保持简单）。新增 3 项回归（配置规范化、自动整理只处理高置信度且任务执行、界面开关三处接线），完整 unittest 349 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 修复自动整理“新增文件无法解析网盘路径，跳过 N 项”：此前用相对监控任务根目录的 `remote_rel` 直接调用 `_walk_existing_folder`（从挂载根 `/115` 开始找），少了一级任务根目录导致解析失败；改为先 `join_remote_path(scan_path, remote_rel)` 拼完整路径，再经 `resolve_provider_relative_path` 转成挂载根相对路径后按段解析父目录（`/115/自存电视剧/问心 (...) → 自存电视剧/问心 (...)`），条目 path 也统一用挂载根相对路径。新增断言（父目录按挂载根相对路径解析），完整 unittest 349 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 修复自动整理“新增条目无高置信度自动匹配”：此前自动整理把新增的**单个媒体文件**作为识别条目，标准刮削文件夹（如 `云秀行 (2026) {tmdbid-239901}`）里的英文文件名搜不出中文标题；改为按父文件夹分组，把**父文件夹作为识别条目**传入（文件夹名含标准标题+年份），并解析祖父目录 ID 作为 parent_id，识别输入与手动选择文件夹一致。新增断言（扫描条目为文件夹、父/祖父目录按挂载根相对路径解析），完整 unittest 349 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 发布元数据更新至 `0.6.0`，同步 version.json、CHANGELOG、README 与当前交接状态，收录本会话全部批量整理、识别核心升级、监控自动整理与交互优化改动（详见 CHANGELOG）。完整 unittest 349 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 端到端验证待补。
- 2026-08-13 | `main` 未提交 | “需手动监控”提示改为可查看、可操作：(1) 修复 `get_manual_required_monitor_scopes` 一直返回空列表的根因——事件表存完整网盘路径（`/115/Media/...`），原逻辑直接 normalize 当挂载相对路径匹配导致上下文解析失败，现先经 `resolve_provider_relative_path` 转成挂载相对路径，并返回 `old_path/new_path/created_at` 明细；(2) 新增 `GET /monitor/manual-required?task_name=` 接口；(3) 监控任务卡片上的“需手动监控 N”改为可点击按钮，弹出明细弹窗（逐条显示路径与事件号），提供“重新扫描”按钮触发 `startMonitorTask` 自动清除。新增 4 项回归（明细字段、接口查询、前端接线、监控同步旧断言更新），完整 unittest 351 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复刮削页 URL 参数污染其它 tab：此前刮削把 `provider/path/cid` 写成全局哈希参数，tab 路由切换会保留这些参数，导致监控页链接也带上了刮削参数。改为按 tab 归属管理——`url-sync.js` 新增 `TAB_OWNED_PARAMS`（scraper 拥有 provider/path/cid），`buildHashWithTab` 切到其它 tab 时删除非本 tab 自有参数；`syncScraperLocationHash` 仅在当前 `tab=scraper` 时写入。新增 1 项行为回归（node 真实调用 buildHashWithTab 验证监控页哈希被清理、刮削页保留参数），完整 unittest 352 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；需 Docker 重建后刷新网页生效。
- 2026-08-13 | `main` 未提交 | “需手动监控”明细升级与容器验证：明细弹窗改为逐条展示事件号、操作类型（`operation` 字段）、旧路径→新路径与处理提示；修复前端任务名双重编码导致明细为空；已在运行中的 `115-media-hub-test` 容器完成重建与演示事件验证（事件 #199，任务“电视剧”）。CHANGELOG/README 已记录该项更新，版本保持 `0.6.0` 不变（未推送）。
- 2026-08-13 | `main` 未提交 | 油猴脚本兼容 iOS（Userscripts）使用：`GM_getValue/GM_setValue` 改为兼容异步 Promise 并把任务/密钥预载入内存，移除同步读取导致的“任务为空、密钥变 `[object Promise]`”；`GM_xmlhttpRequest` 缺失或同步抛错时自动走 `fetch` 兜底（torrent 二进制下载同步补兜底）；http 页面无 WebCrypto 时用纯 JS SHA-256/HMAC 签名兜底（RFC 4231 向量验证）。新增 7 项 node 回归测试，完整 unittest 359 项、compileall、node --check 与 git diff --check 均通过；Docker 未重建，iOS 真机实测待补。下一步：iOS 安装 Userscripts 后真机验证配置任务与磁力推送。
- 2026-08-13 | `main` 未提交 | 油猴脚本配置入口重构：取消全局右下角悬浮窗（避免影响所有网页），任务管理器入口全部收进“115”按钮——未配置任务时点击任意“115”按钮直接打开任务管理器（不再只弹“没有可用任务”），已配置时任务选择器底部新增“任务管理”按钮；桌面端保留油猴菜单入口。新增无任务直开管理器、选择器管理按钮、无浮窗回归测试。下一步：iOS 真机验证配置与推送。
- 2026-08-13 | `main` 未提交 | 修复单任务时“115”按钮直接推送导致无法进任务管理器的漏洞：移除 `chooseTask` 单候选快捷返回，即使只有一个任务也始终弹出任务选择器（底部保留“任务管理”入口），保证 iOS 无油猴菜单也能随时进入配置。新增单任务仍弹选择器且可选中推送的回归测试。下一步：iOS 真机验证配置与推送。
- 2026-08-13 | `main` 未提交 | 刮削标题清洗规则扩展：复合短语新增“国语配音”“中文字幕/中文配音/中文音轨/中文版”；新增 `SCRAPER_STANDALONE_NOISE_WORDS` 独立词表（中文/国语），按词边界清洗并参与纯噪声过滤，避免误伤“我的中文老师”这类含“中文”的真实片名。新增 1 项回归，完整 unittest 362 项、compileall 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后实测真实文件名识别。
- 2026-08-13 | `main` 未提交 | 刮削页交互与保留标签升级：(1) 批量整理面板“搜索绑定”结果行复用 `renderPoster` 显示 TMDB 封面；(2) 手机端选择区网格由 5 列改 6 列并移除 360px 的 4 列覆盖，区间选择/重命名/复制/移动/删除/识别整理 6 个主按钮保持一行；(3) 命名选项“目录处理”拆为“文件夹结构”（Season 子文件夹 → 写入 TMDB ID → 同步重命名）与“文件清理”（删除广告文件）；(4) 保留标签新增“语言/字幕”两组（国语/粤语/双语/台配、中字/简中/繁中/英字/双语字幕/内封外挂中字等，整短语优先且词边界安全，不误伤“我的英语老师”类片名），并修复 `AAC.10bit` 把 10bit 误判成 1.0 声道的问题。新增 11 项回归，完整 unittest 375 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测批量搜索封面、手机工具栏一行与新标签保留效果。
- 2026-08-13 | `main` 未提交 | 批量整理任务名改为展示 TMDB 绑定信息：`create_scraper_job_from_plan` 对批量任务按“实际执行动作涉及的条目”生成任务名并存入 `tmdb.title`——单条显示“标题 (年份)”（如 云秀行 (2026)），多条显示“首部标题 等 N 项”（N=实际执行条数），无条目信息时维持“批量整理”；刮削页任务记录与任务中心刮削 tab 均自动生效（都读 job.tmdb），旧任务不迁移。新增 5 项回归（单条带年份、多条计数、部分执行按勾选条目、非批量任务保留绑定标题、无条目兜底），完整 unittest 380 项、compileall 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测任务名展示。
- 2026-08-13 | `main` | 发布元数据更新至 `0.6.1`，同步 version.json、CHANGELOG、README 与当前交接状态，收录本会话全部刮削交互优化（批量搜索封面、手机端工具栏一行、命名选项分组、语言/字幕保留标签与 10bit 声道修复、批量任务名 TMDB 展示）及 0.6.0 之后的油猴 iOS 兼容改动；完整 unittest 380 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实浏览器/115 端到端验证待补。
- 2026-08-14 | `main` | 发布元数据更新至 `0.6.2`，同步 version.json、CHANGELOG、README 与当前交接状态，收录三项改动：(1) 任务中心手机端操作按钮可收缩换行不再向左溢出，按钮文案精简，禁用配色不变；(2) 搜索/识别提示与结果副标题的长关键词改为中间省略显示，三处文案容器增加 `overflow-wrap: anywhere` 兜底；(3) 任务历史清理从页面请求拆到后台周期任务（`prune_resource_jobs_if_due`，默认每 10 分钟，`RESOURCE_JOB_PRUNE_INTERVAL_SECONDS` 可调），页面请求只做任务状态恢复。新增 2 项回归测试，完整 unittest 382 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实浏览器移动端视口与容器行为验证待补。下一步：重建容器后实测手机端任务按钮一行/换行、长磁力提示条无横向溢出，并观察日志确认后台清理按间隔执行。
- 2026-08-14 | `main` 未提交 | 修复导入任务列表“自动订阅/来源”标签日间配色过浅：任务卡片来源标签此前用 `text-violet-200`（浅紫文字），日间主题下在白色卡片上几乎看不清；改为与状态徽标一致的主题类 `bg-violet-500/15 text-violet-300 border border-violet-500/20`，日间模式自动套用既有 `#6d28d9` 深紫文字覆盖（夜间保持淡紫不变）。新增 1 项回归（标签类名不再含 text-violet-200、日夜覆盖存在），完整 unittest 383 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-16 | `main` 未提交 | 刮削页“扫描监控”功能：选择栏新增“扫描监控/刷新 STRM”按钮，勾选文件夹（扫其子树）或文件（映射到父目录）后入队手动监控任务；后端队列 payload 扩展 `savepaths`（去重、上限 50）与 `provider`，`run_monitor_task` 支持多起始目录扫描（`refresh_source_label="指定目录扫描"`，逐起始目录标记首层 dirty，清理与索引替换按范围并集限定，超限合并回退全量），新增 `queue_monitor_dir_scan` 与 `POST /monitor/scan`（跨任务自动分组、未匹配返回明确提示）。新增后端 8 项 + 前端 6 项回归，完整 unittest 451 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测按钮交互与子树扫描。
- 2026-08-16 | `main` 未提交 | 方案一可行性验证（115 最近操作接口）：实测 `life.115.com/api/1.0/web/1.0/life/recent_operations`（按行为分组）与 `webapi.115.com/behavior/detail`（历史流水）可用，事件带 `file_id/parent_id/parent_name/时间/大小`；用户受控操作复测完成——复制（`copy_folder`/type 18）、重命名（`file_rename`/24、`folder_rename`/20）、移动（type 6/5）出现在 `recent_operations`，删除（type=22）与上传（type=2，`SteamSetup.exe` 实测）仅出现在 `behavior/detail`，均经 `get_info` 验证（删除返回“文件不存在或已删除”、上传正常返回且 `cid`=父目录）；`get_info` 返回 `cid/pid` 可直接逐级反查完整路径；接口不支持服务端过滤/分页，`limit` 有效，偶发一次空返回需容错。结论与落地建议（双源轮询 + type 代码表 + get_info 路径反查 + 复用 savepaths 局部刷新通道）写入 `docs/superpowers/specs/2026-08-16-115-recent-operations-feasibility.md`。下一步：与用户确认是否按此方案实现轮询功能。
- 2026-08-14 | `codex/pr5-cli-merge` 已提交（未 push） | 合并外部贡献 PR #5（CLI 扩展）并适配当前主分支至 0.7.0：新增宿主机 `cli.py`（25 个子命令，登录改为 MH_USERNAME/MH_PASSWORD 或交互输入，不再静默用默认口令；修复 search --cancel、subscribe remove→/subscription/delete、monitor start/stop/remove 按名称、scrape jobs-create 三步流）；后端 scraper rename/move/copy/delete 增量支持 path/dest 入参（仅 115，复用分页解析），identify/rename-plan 支持纯路径条目；新增发现源注册表（discovery_base/discovery_registry）与 requirements-cli.txt、README 用法。审查修复共 12 项：logs --tail、resource delete --id、115 分页 id 字段文件判定、browse tree 只递归文件夹、路径解析错误透传、jobs-create 未就绪先列冲突、ID 非法中文提示，以及 subscribe add 保存路径自动推断、schedule/settings/tmdb/watchlist 输入校验、channels sync 文案、容器名自动识别、115 规范化函数统一。新增 5 个测试文件，完整 unittest 436 项、compileall 与 git diff --check 均通过。Docker 已重建（daemon 曾未运行，本次启动 Docker Desktop 后 `docker compose up -d --build`），正式容器 `115-media-hub-test` 已用当前代码重启；另用临时容器（全新默认配置、18081 端口）完成 CLI 冒烟：登录/status/version/search/providers/jobs/sources list/daemon 自动识别/scrape 路径接口错误链路均通过，Cookie 文件 0600，临时容器已清理。真实 115 刮削路径操作仍待有真实 Cookie 的环境验证；正式容器密码为 PBKDF2 哈希，CLI 登录需用户真实密码。
- 2026-08-16 | `main` 未提交 | 方案一（115 事件自动同步）决策：**不采用、不实施轮询服务**，调研转为知识储备。两份调研文档合并为 `docs/superpowers/specs/2026-08-16-115-life-events-knowledge.md`（参考项目调研、life/behavior 双接口实测、type 代码表、用户受控操作复测、若将来实施的设计备忘），删除 `plans/2026-08-16-115-life-event-sync.md` 与旧的 feasibility 文档（内容全部并入新文档）。线上兜底继续用已实现的刮削页“扫描监控/刷新 STRM”按钮。下一步：Docker 重建后浏览器实测方案二按钮交互与子树扫描。
- 2026-08-16 | `main` 未提交 | 批量整理体验优化：(1) 入口按钮统一为“批量整理”（弹窗/任务名已为此名）；(2) 命名选项重排为「文件夹（重命名文件夹/创建 Season 子文件夹/写 TMDB ID）→ 文件命名 → 文件清理（删除广告文件）」；(3) 新增“文件命名方式”三档 `file_name_mode`（standard 按识别结果重命名 / clean 仅清理广告网址与站点名保留原始命名 / keep 保持原名不重命名），keep/clean 下文件不移动（文件夹重命名照常、聚集剧集仍按 Season 子文件夹设置移入、文件名保持/仅清洗）；(4) 选项记忆存服务端并按网盘分开：新增 `scraper_batch_preferences` 表、`GET/POST /scraper/{provider}/batch/preferences`（白名单归一化、空 options 清除记忆）、前端加载/防抖 400ms 保存/“恢复默认”按钮。新增后端 11 项 + 前端 3 项回归并更新 2 项旧断言，完整 unittest 465 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 批量整理实测待补。下一步：Docker 重建后浏览器实测三档命名、选项记忆跨网盘恢复与 Season 子文件夹组合行为。
- 2026-08-16 | `main` 未提交 | 监控任务“新增资源自动刮削整理”支持按任务配置整理选项（方案 A）：弹窗勾选开关后展开“自动整理选项”三段（文件夹：重命名/Season 子文件夹/写 TMDB ID；文件命名：保持原名/仅清理广告/标准重命名 + 标题语言/默认季号/季集识别/保留细节；文件清理：删除广告文件）；后端 `normalize_task` 白名单新增 `auto_scrape_options`（非 dict 忽略），`_auto_scrape_new_media_items` 用任务选项（未配置时保持原默认 `title_language=zh, delete_ad_files=False`）替换写死选项，经 `_normalize_scraper_batch_preferences` 白名单归一化。新增后端 2 项 + 前端断言扩展，完整 unittest 466 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 自动刮削按任务选项整理待测。下一步：Docker 重建后浏览器实测监控任务选项保存/恢复与自动整理三档行为。
- 2026-08-17 | `main` 未提交 | 补齐 CLI 支持与文档（0.7.1 网页端新能力的命令行入口）：`scrape` 新增 `batch-preferences get|set|clear`（读写/清除某网盘批量整理偏好，`--provider` 指定网盘）；`scrape rename-plan` 新增 `--file-name-mode keep|clean|standard`、`--no-rename-folders`、`--no-season-subfolder`、`--include-tmdb-id`、`--delete-ad-files`、`--title-language`、`--season`、`--episode-mode`、`--preserve-file-info` 与 `--options-json`；`monitor add` 新增 `--auto-scrape-on-new` 与 `--auto-scrape-options-json`。README CLI 段落与 CHANGELOG/version.json 同步更新；handoff「记录规则」新增“涉及 CLI 能力的改动须同步补齐 cli.py 支持与 README CLI 文档”。新增 7 项 CLI 回归（batch-preferences get/set/clear、非法子动作/JSON 干净报错、rename-plan 选项透传、monitor add 自动刮削选项），完整 unittest 473 项、compileall、git diff --check 均通过；Docker 未重建。下一步：用户确认后提交（0.7.1 内容追加），Docker 重建后实测 CLI 命令与页面效果。
- 2026-08-17 | `main` 未提交 | 发布元数据更新至 `0.7.2`，同步 version.json、CHANGELOG、README 与当前交接状态，收录 CLI 补齐（batch-preferences 读写/清除偏好、rename-plan 命名选项、monitor add 自动刮削选项与文档）及交接记录规则更新。完整 unittest 473 项、compileall、git diff --check 均通过；Docker 未重建，CLI 命令与页面效果待容器重建后实测。下一步：Docker 重建后实测 CLI 命令与批量整理/自动刮削页面效果，确认后推送。
- 2026-08-17 | `main` 未提交 | 目录树任务化改造（`docs/superpowers/plans/2026-08-17-115-quick-export-dir-tree.md`）：废弃旧 `trees` 静态树源/定时/MD5 全量模式，改为“目录树任务”模型——每个任务绑定一个 115 文件夹，调用官方 `files/export_dir` 生成树文件（网盘根目录，命名 `目录树-路径段…`），固定执行“导出→删旧（回收站）→原地重命名”，随后用远端文件 sha1 与上次比对，未变化则跳过下载/解析/写 STRM；默认增量，任务卡片提供“全量重写”；清理残留按任务 scope（folder_path 子树）。真实账号校准完成（根级+二级两次导出）：响应字段、UTF-16 首行 `|——根目录/|——父级`、根目录落盘、sha1 可用均确认，自动填充公式锁定为“排除层级=1 + 前缀=父级链（根级为空）”；完整链路实测 982 个媒体文件正确生成 STRM。设置页移除目录树配置，配置项迁移到同步页（sha1 跳过/清理残留全局开关）；CLI `tree list|create|run|full|jobs` 与 README/CLI-API-AUDIT 同步。新增 tests/test_115_export_dir.py、test_tree_tasks.py、test_tree_page_frontend.py，完整 unittest 496 项、compileall、改动 JS `node --check`、git diff --check 均通过。Docker daemon 权限不足未重建。下一步：启动 Docker 后 `docker compose up -d --build` 实测页面按钮/任务流转与 CLI 命令，确认后决定是否提交。
- 2026-08-17 | `main` 未提交 | 修复目录树任务化改造导致的容器重启循环：移除 tree 定时调度时遗漏了 `startup.py` 里 `asyncio.create_task(scheduler())` 对已删除函数的引用，容器启动事件抛 NameError 后退出、Docker 反复重启。已删除该行并用真实配置本地完整跑通 startup 事件（FULL_STARTUP_OK），新增源完整性回归测试，完整 unittest 497 项、compileall、git diff --check 通过。下一步：用户重建镜像验证容器不再重启。
- 2026-08-17 | `main` 未提交 | 目录树任务页布局改为订阅任务风格：页面头部“+ 新增目录树任务 / 全部同步”按钮 + 任务列表卡片；新增/编辑统一走弹窗（文件夹、树文件名、父文件夹路径前缀、排除层级，保存后进列表）；文件选择器复用现有订阅目录浏览（`subscription/folders.js` 增加通用回调参数 `openSubscriptionFolderModal(callback, providerOverride)`，订阅原逻辑不变）；移除页面内联新增表单与自建文件夹选择弹窗。后端新增 `POST /tree/tasks/{id}` 编辑接口（校验文件夹/防同名冲突）。新增前端回归（模板标记 + 弹窗保存 POST 断言），完整 unittest 498 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后实测页面。
- 2026-08-17 | `main` 未提交 | 目录树任务弹窗参数改为纯自动推导：父文件夹路径前缀（父级链，根级为空）与排除层级（固定 1）由所选文件夹按解析规则自动填充，前端只读、后端 create/update 均忽略手动传入值；树文件名仍可编辑；CLI create 移除 --prefix/--exclude。修复任务弹窗 z-50 被目录浏览弹窗（z-56）覆盖的层级问题。新增路由级回归（create/update 忽略手动 prefix/exclude 并重新推导）与前端断言，完整 unittest 501 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 修复目录树生成后“重命名未完成/文件不存在”的根因：115 重命名接口会对不带扩展名的目标名**自动补 .txt**（实测响应 `data: {fid: "目录树-x.txt"}`），原逻辑按不带后缀的名字校验与按名查找，永远失败。现统一用远程实际名 `树名 + .txt`（`_tree_file_remote_name`）做删除/重命名/校验/按名读取；重命名后校验改用 `files/get_info` 按 file_id 直查（即时、不受目录列表延迟影响），并在导出完成后先等文件就绪再加退避，减少对 115 的重复访问（对齐官方客户端“轮询状态+加载窗”行为）。真实账号复验 REPLACE_OK/sha1/下载通过。新增 get_115_file_info 封装与相关单测，完整 unittest 505 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 目录树任务页 UI 整理：(1) 进度条内嵌进任务卡片头部，按任务流程阶段实时推进，任务结束后把进度重置为 0/就绪（此前结束后卡在最后百分比）；(2) 删除独立的“生成记录”卡片，日志与记录合并为单一日志框，并按新流程排版阶段日志（提交导出→等待官方生成→替换网盘树文件→sha1 对比→下载并解析/全量重写→结果汇总：解析/写入/清理/耗时/sha1）；(3) 修复日间主题配色：theme-day 的 `text-white` 覆盖漏了 `#page-task`，已补上（白字在浅色卡片上不可见）。新增进度重置与日间 CSS 回归断言，完整 unittest 506 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 目录树日志改为订阅任务同款分割线风格并加入分步计时：`task-divider` 起止行（任务开始/执行成功/未变化跳过/执行失败 + 总用时），主体按【生成目录树】（提交导出/等待官方生成/替换网盘树文件/小计）与【解析目录树】（sha1 对比/下载并解析/小计）两大部分排版，末尾输出“步骤耗时：…”与“总用时：…”；全部同步同样补起止分割线与逐任务耗时。新增计时汇总单测，完整 unittest 508 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 旧目录树配置字段一次性清理：启动时若 settings.json 仍含 `trees/sync_mode/check_hash/cron_hour/last_hash`，用归一化配置重写一次并在任务日志提示，用户无需手动处理（内存中早已忽略，本次是落盘清理）。设置页章节序号重新编号：移除 3/4 目录树区块后，TG 订阅源=3、PanSou=4、网络代理=5、TMDB=6、通知推送=7、后台安全=8（标题与 order 同步）。新增清理单测与模板断言，完整 unittest 510 项、compileall、git diff --check 均通过。
- 2026-08-17 | `main` 已提交 | 本轮目录树任务化改造全部改动提交（CLI 同步补齐 `tree update/defaults` 并更新 README/CLI-API-AUDIT；完整 unittest 514 项、compileall、node --check、git diff --check 均通过；Docker 重建与真实页面复核留待下一步）。
- 2026-08-17 | `main` 未提交（功能代码已提交 `b2a27d2`） | 发布元数据更新至 `0.8.0`，同步 version.json、CHANGELOG、README 与当前交接状态，收录目录树任务化改造（官方 `files/export_dir` 生成树文件 + 远端 sha1 跳过 + 任务化 UI + CLI `tree` 命令、设置页配置迁移与旧字段自动清理、容器启动重启循环与 115 重命名自动补 `.txt` 根因修复）。完整 unittest 514 项、compileall、node --check、git diff --check 与 version.json JSON 解析均通过；Docker 未重建。下一步：Docker 重建后实测目录树任务页按钮/任务流转与 CLI `tree` 命令，确认后提交推送。
- 2026-08-18 | `main` 未提交 | 磁力/电驴离线任务改为官方云下载任务完成检测驱动：新增 115 `lixian.115.com` 的 `task_lists` 查询（`list_115_offline_tasks`，复用 115 限速锁与 Cookie 健康上报），提交后记录磁力 `btih`/电驴哈希与目标目录，后台轮询器按“哈希或 URL 匹配 + `wp_path_id` 一致”等待任务完成（status=2）再触发监控扫描；失败/超时（默认 12 小时，可配置）置 failed 不自动扫描，errcode 10008 重复任务不等待并保留手动刷新；“监控触发延时”保留为首次轮询前最小等待；定时监控扫描在存在未完成离线任务时自动顺延，完成后由轮询器补发；任务中心磁力/电驴任务卡片新增 115 下载进度条（百分比 + 已下载/总大小，日/夜主题适配）。新增只读诊断接口 `GET /resource/offline/tasks` 与 CLI `offline list [--page N]`，README/CLI-API-AUDIT 同步。新增 tests/test_resource_offline_completion.py、test_resource_offline_progress_frontend.py 与 CLI 回归，完整 unittest 538 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后用真实 115 账号实测磁力/电驴哈希匹配（重点核实电驴任务 `info_hash` 与 ed2k 哈希关系）、慢速任务进度展示与 cron 顺延、秒存立即扫描、死种超时置失败。
- 2026-08-19 | `main` 已提交 | 修复目录树导出超时并调整“全部同步”按钮：官方导出等待时长从固定 600 秒提高到默认 1800 秒（`TREE_EXPORT_TIMEOUT_SECONDS` 可调），超时后任务置 failed，错误信息给出明确手动指引（到 115 根目录找到服务端命名的新树文件，删除旧标准名文件并改名，再点「下载并生成」）；原「全部同步」按钮改名为「下载并生成」，不再按 sha1 跳过，改为直接下载已存在的标准名树文件并进入生成，覆盖“导出超时后手动改名、点同步却拿旧文件/不生成”的死循环。顺带把日志「生成目录数」统一纠正为「生成目录树」。新增 4 项回归（超时指引落库、force_fetch 强制下载、未强制时仍跳过、按钮与 JS 提示改名），完整 unittest 584 项、compileall、node --check、git diff --check 均通过。下一步：Docker 重建后实测超时提示与「下载并生成」流程。
- 2026-08-19 | `main` 已提交 | 发布元数据更新至 `0.9.1`，同步 version.json、CHANGELOG、README 与当前交接状态，收录目录树导出超时处理与「下载并生成」按钮调整；完整 unittest 584 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后实测超时提示与「下载并生成」流程，确认后推送。
